## Handle Errors in Serverless Apps with AWS Step Functions and AWS Lambda

How-to guide

### Read `changelog.md` for instructions on how-to handle error like a PRO in serverless apps with `AWS Lambda` and `Step Functions`
